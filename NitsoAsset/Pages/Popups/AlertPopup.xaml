<?xml version="1.0" encoding="UTF-8" ?>
<pages:PopupPage xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 x:Class="NitsoAsset.Pages.Popups.AlertPopup" CloseWhenBackgroundIsClicked="False">
    <!--You can set an animation in the xaml file or in the csharp code behind-->
      <pages:PopupPage.Animation>
        <animations:ScaleAnimation PositionIn="Center" PositionOut="Center" ScaleIn="1.2" ScaleOut="0.8" DurationIn="400" DurationOut="300" EasingIn="SinOut" EasingOut="SinIn" HasBackgroundAnimation="True" />
    </pages:PopupPage.Animation>

    <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand" Margin="0" Spacing="0">
        <Frame IsClippedToBounds="true" BackgroundColor="White" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" HasShadow="false" Margin="40" Padding="0,20,0,0" CornerRadius="10">
            <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Spacing="5">
                <StackLayout Padding="10" Spacing="1" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                    <Label Text="{Binding AlertMessage}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Medium" />
                </StackLayout>
                <Button Style="{DynamicResource BlueButton}" BackgroundColor="{DynamicResource Purple}" CornerRadius="0" Command="{Binding CloseCommand}" VerticalOptions="Start" Margin="-20,15,-20,0" Text="Ok"  />
            </StackLayout>
        </Frame>
    </StackLayout>
</pages:PopupPage>